{% if js_libs is defined %}{{ js_libs }}{% endif %}
<script nonce="{{ constant('CSP_NONCE') }}">$.API = { API_PATH: "{{ constant('PUBLIC_API_V0_PATH') }}" };</script>
{% if default_js %}
  {{ export_vars({
    'PRINTABLE_ASCII_PATTERN': constant('PRINTABLE_ASCII_PATTERN'),
    'signedIn': signed_in,
    'discordInviteLink': constant('DISCORD_INVITE_LINK'),
  }) -}}
{% endif %}
{% for url in js %}
  <script src='{{ url }}'></script>
{% endfor %}
