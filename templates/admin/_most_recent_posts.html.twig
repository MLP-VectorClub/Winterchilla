{# @var recent_posts \App\Models\Post[] #}
{# @var wrap string #}
{% if wrap is not defined or wrap %}
<ul>
{% endif %}
	{% for post in recent_posts %}
		{{ post.li }}
	{% endfor %}
{% if wrap is not defined or wrap %}
</ul>
{% endif %}
