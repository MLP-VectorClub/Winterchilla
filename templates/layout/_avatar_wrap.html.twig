<div class='avatar-wrap provider-{{ (current_user.get_avatar_provider ~ 'deviantart') ~ current_user.getVectorAppClassName }}' $for>
	<img src='{{ current_user.avatar_url }}' class='avatar' alt='avatar'>
</div>
