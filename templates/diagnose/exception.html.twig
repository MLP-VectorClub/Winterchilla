{# @controller \App\Controllers\DiagnoseController::exception #}
{# The syntax here is broken on purpose to allow testing how the application behaves when this page is rendered #}
{{ undefined_variable }}
{% syntax error {{
